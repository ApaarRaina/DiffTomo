[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "difftomo"
version = "0.1.0"
description = "Differentiable library for tomography"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "torchvision",
    "torch"
]